<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net8.0-android</TargetFramework>
    <SupportedOSPlatformVersion>26.0</SupportedOSPlatformVersion>
    <OutputType>Exe</OutputType>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    
    <!-- Pigilan ang NETSDK1202 EOL warning/error -->
    <CheckEolWorkloads>false</CheckEolWorkloads>

    <!-- Pigilan ang pagbura ng reflection types ng Prowl -->
    <PublishTrimmed>false</PublishTrimmed>
    <RunAOTCompilation>false</RunAOTCompilation>

    <!-- ABIs na karaniwang ginagamit ng modern Android devices -->
    <RuntimeIdentifiers>android-arm64;android-arm;android-x64</RuntimeIdentifiers>
    <ApplicationId>com.gasukinani.prowlmobile</ApplicationId>
    <ApplicationVersion>1</ApplicationVersion>
    <ApplicationDisplayVersion>1.0</ApplicationDisplayVersion>
  </PropertyGroup>

  <ItemGroup>
    <!-- Reference sa umiiral na Prowl.Runtime -->
    <ProjectReference Include="..\Prowl.Runtime\Prowl.Runtime.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Silk.NET SDL & GLES Bindings para sa Android -->
    <PackageReference Include="Silk.NET.Windowing.Sdl" Version="2.20.0" />
    <PackageReference Include="Silk.NET.Windowing.Sdl.Android" Version="2.20.0" />
    <PackageReference Include="Silk.NET.OpenGL.Extensions.EXT" Version="2.20.0" />
    <PackageReference Include="Silk.NET.Input" Version="2.20.0" />
    <PackageReference Include="Silk.NET.OpenAL.Soft.Native" Version="1.23.1" />
  </ItemGroup>
</Project>
